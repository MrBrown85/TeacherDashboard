{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit *)",
            "command": "jq -r '.tool_input.command' | grep -qE '(^|[^-[:alnum:]])git commit([[:space:]]|$)' && bash scripts/format-staged.sh || true",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
