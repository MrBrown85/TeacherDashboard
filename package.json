{
  "name": "teacher-dashboard",
  "version": "1.0.0",
  "description": "Standards-based gradebook for BC teachers",
  "private": true,
  "scripts": {
    "dev": "npx serve -l 8347 --no-single",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "prettier --write '**/*.{js,css,html,json,md}'",
    "format:check": "prettier --check '**/*.{js,css,html,json,md}'"
  },
  "devDependencies": {
    "prettier": "^3.2.0",
    "vitest": "^1.6.0"
  }
}
